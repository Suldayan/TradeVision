spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:latest://localhost/integration-test-db
  jpa:
    hibernate:
      ddl-auto: update
  test:
    containers:
      enabled: true